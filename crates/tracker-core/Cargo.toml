[package]
name = "tracker-core"
version = "0.1.0"
edition = "2021"
description = "Tracker monorepo shared core: prerequisite graph, progress, file I/O, config skeleton"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
