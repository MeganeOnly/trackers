{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default permissions for main + sticky windows",
  "windows": ["main", "sticky"],
  "permissions": [
    "core:default"
  ]
}
