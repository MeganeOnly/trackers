{
  "name": "tracker-core",
  "version": "0.1.0",
  "private": true,
  "description": "Tracker monorepo shared TS core (pure logic, no React)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
